# Arch Hardened - /etc/default/ntp

# -4 Force DNS resolution of host names to the IPv4
# -g Allow hwclock to be set to any value without restriction
# -q Exit just after the first time the clock is set
# -x Increase step threshold from 128s to 600s

NTPD_OPTS='-4 -g -q -x'
