# ~/.config/firejail/xed.local
#
# github.com/kuladog
#

noblacklist ${HOME}/

# apparmor - makes settings immutable
# net none - makes settings immutable
caps.drop all
machine-id
protocol unix

private-tmp
