abi <abi/4.0>,

include <tunables/global>

profile bash /bin/bash {
  capability sys_ptrace,
  capability chroot,
  capability setuid,
  capability setgid,

  /home/** rw,
  /tmp/** rw,
  /etc/passwd r,
  /etc/shadow r,
  /etc/group r,

  deny network raw,
  deny network inet6,

  deny /**
}
