include <tunables/global>

profile dhclient /sbin/dhclient {
  network inet udp,
  capability net_bind_service,
  capability setgid,
  capability setuid,

  # Allow DHCP config read/write
  /etc/dhcp/** r,
  /var/lib/dhcp/** rw,

  deny /**
}
