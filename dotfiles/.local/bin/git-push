#!/usr/bin/env bash

#  Command: git-push
#  Automates 'git push' from a local directory
#

if [[ -z "$1" ]]; then
	read -rp "What's your commit? " commit
else
	commit="$*"
fi

git add .
git commit -m "$commit"
echo
git push -u origin main
