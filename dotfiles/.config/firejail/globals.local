# ~/.config/firejail/globals.local

blacklist /usr/local/bin
blacklist /usr/local/sbin

seccomp
ipc-namespace
machine-id
netfilter

private-tmp

caps.drop all
nonewprivs
noroot
nogroups
noautopulse
noprinters
noinput

noexec ${RUNUSER}
noexec /tmp

restrict-namespaces
