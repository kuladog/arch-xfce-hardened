# ~/.config/firejail/globals.local

blacklist /usr/local/bin
blacklist /usr/local/sbin

private-tmp

apparmor
caps.drop all
ipc-namespace
machine-id
netfilter
seccomp

noroot
nogroups
nonewprivs
noinput
noautopulse

noexec ${RUNUSER}
noexec /tmp
