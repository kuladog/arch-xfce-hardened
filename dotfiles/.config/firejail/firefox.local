# ~/.config/firejail/firefox.local

include globals.local

noexec ${DOWNLOADS}

private-bin firefox,env,sh,bash,which,dbus-launch,dbus-send
private-etc firefox,mime.types,os-release,@tls-ca,@x11

protocol inet
seccomp.block-secondary
