# ~/.config/firejail/global.local

blacklist /usr/local/bin
blacklist /usr/local/sbin

seccomp
ipc-namespace
machine-id
nonewprivs

private-tmp

caps.drop all
netfilter
noroot
nogroups

noexec ${RUNUSER}
noexec /tmp

